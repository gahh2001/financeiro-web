import { atom } from "jotai";

export const googleIdAtom = atom("102519029829209703887" as string | null);
export const saldo = atom(0 as number);
export const modalPlanajamento = atom(false as boolean);